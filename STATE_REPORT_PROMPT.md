Read CLAUDE.md first. Then produce a STATE REPORT.
Do not change any files. Do not run git add, commit, or push.
Do not fix anything. Report facts only.

1. git --no-pager status --short
2. git branch --show-current
3. git --no-pager log --oneline -5
4. Current version from package.json
5. For each modified or untracked file: one line — what it is,
   and whether it belongs in version control
6. Any file containing: sk-ant, api_key, password, secret, Bearer
7. Anything in CLAUDE.md that is now out of date
8. What CLAUDE.md says is the next roadmap item

Output as plain text I can copy.